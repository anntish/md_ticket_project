version: 2

models:
  - name: stg_ticket_offers
    description: >
      Актуальные данные API
    config:
      elementary:
        timestamp_column: created_at
    columns:
      - name: response
        description: >
          ответ API
        tests:
          - not_null:
              config:
                severity: error
                tags: [ "dqc" ]

      - name: request
        description: >
          запрос API
        tests:
          - not_null:
              config:
                severity: error
                tags: [ "dqc" ]
      - name: aviasales_id
        tests:
          - unique:
              config:
                severity: error
                tags: [ "dqc" ]